# v0.2 Status Snapshot - 2026-06-04

## Latest Update - 2026-06-05

- Added OCR caption-quality filtering after real Teams acceptance feedback:
  - `src\teams_caption_translator\caption_filter.py` rejects date/time metadata, numeric-only lines, digit-started avatar labels, Teams-style internal speaker IDs, and symbol-heavy OCR gibberish before text reaches DeepSeek.
  - `pipeline.py` now applies this filter to both the current OCR preview and the translation pipeline, reducing dirty transcript lines and wasted tokens.
  - Added `tests\test_caption_filter.py` to cover the observed bad cases: `05月14日`, `13.37`, `10下`, and `ロ ヽ のII...` OCR noise.
  - Manual test-function run passed for caption filter, caption parser, and stabilizer tests.
  - Rebuilt the Windows installer and release zips after this filter change.
  - Latest installer SHA256: `2634E355DCBDB9678962BF725A6866E5C40A480ECF89ED38C22D784CB8AAC68A`.
  - Latest Windows release zip SHA256: `A91A778B5ACBCC8E1555F3077C4DD3ACE01DD78D61F7CB8FA2ECC27371F9A770`.
  - Latest timestamped macOS source package: `release\TeamsCaptionTranslator-v0.2.0-macos-source-20260606_144240.zip`.
  - Latest timestamped macOS source package SHA256: `B9A86E2B751D250D7193A208C48856C2D3AAB470683D5FA346F9B12E0BBEA6BA`.
  - `scripts\verify_release.ps1 -OcrSmoke` passed after rebuilding.
  - `scripts\audit_release_readiness.ps1 -RunSmoke` passed all actual package, document, install, first-run UI, OCR-preparation, uninstall, model, and macOS-source checks; it still fails only because this host has no Windows Sandbox.
- Added `docs\FINAL_ACCEPTANCE_MATRIX.md` as the source of truth for the full Windows + macOS completion standard.
- Replaced the mojibake-generating release-document template in `scripts\prepare_release_package.py` with readable UTF-8 Chinese text.
- Replaced the mojibake root `README.md` with a readable project overview covering Windows distribution, macOS build status, support bundles, and final acceptance criteria.
- Rewrote `scripts\prepare_release_package.py` again after finding a mojibake regression in the release-document generator. The generator now emits readable UTF-8 Chinese release docs and keeps the machine-readable manifest.
- Added generated `RELEASE_MANIFEST.json` to the Windows release folder and zip. It records the package platform, installer hash, Python-free ordinary-user requirement, bundled OCR models, support-bundle scripts, and macOS validation status.
- Updated `scripts\audit_release_readiness.ps1` so the release manifest must exist, be included in the zip, parse as JSON, match the installer hash, declare Windows as the current package platform, and mark macOS as not validated.
- Added macOS `RELEASE_MANIFEST.json` generation to `scripts\prepare_macos_release.sh`. It records the `.dmg` hash, no-Python ordinary-user requirement, support-bundle script, and macOS validation status.
- Updated `docs\MACOS_PACKAGING.md`, `docs\FINAL_ACCEPTANCE_MATRIX.md`, and `scripts\audit_release_readiness.ps1` to require/check the macOS release manifest.
- Strengthened macOS OCR model packaging:
  - `scripts\build_macos.sh` now checks the selected Python interpreter is Python 3.10 or newer before creating `.venv-macos`.
  - `scripts\build_macos.sh` now initializes EasyOCR for Japanese before PyInstaller runs.
  - `scripts\build_macos.sh` now sets `TCT_EASYOCR_MODEL_DIR`, verifies `craft_mlt_25k.pth` and `japanese_g2.pth` before packaging, and fails if either model is missing.
  - `scripts\build_macos.sh` now searches the generated `.app` and fails if either EasyOCR model was not bundled.
  - `scripts\check_macos_release.sh` now verifies the generated `.app` contains both EasyOCR model files.
  - `docs\MACOS_PACKAGING.md`, `docs\FINAL_ACCEPTANCE_MATRIX.md`, and `scripts\audit_release_readiness.ps1` now include/check the macOS bundled-model requirement.
- Added a Windows-side clean source package for Mac builders:
  - `scripts\prepare_macos_source_package.ps1` creates `release\TeamsCaptionTranslator-v0.2.0-macos-source.zip`.
  - The source package excludes `.venv`, `.venv-macos`, build outputs, dist outputs, release artifacts, logs, and local `config.json`.
  - The source package includes `BUILD_ON_MAC.txt` with the three real-Mac commands: `build_macos.sh`, `check_macos_release.sh`, and `prepare_macos_release.sh`.
  - `README.md`, `docs\MACOS_PACKAGING.md`, `docs\FINAL_ACCEPTANCE_MATRIX.md`, and `scripts\audit_release_readiness.ps1` now mention/check this source-package path.
  - `docs\SEND_TO_MAC_TESTER.txt` was replaced with readable UTF-8 Chinese after another mojibake regression was found.
- Added source-package manifest support:
  - `scripts\prepare_macos_source_package.ps1` now writes `SOURCE_PACKAGE_MANIFEST.json` inside the source zip.
  - It also writes `release\TeamsCaptionTranslator-v0.2.0-macos-source.SOURCE_PACKAGE_MANIFEST.json` next to the zip after hashing the package.
  - The manifest records the source package zip name, hash, size, exclusion rules, Mac build commands, expected Mac output, and Python/no-Python requirements.
  - `README.md`, `docs\MACOS_PACKAGING.md`, `docs\FINAL_ACCEPTANCE_MATRIX.md`, and `scripts\audit_release_readiness.ps1` now document/check the source-package manifest.
- Fixed macOS source zip path separators:
  - `scripts\prepare_macos_source_package.ps1` now uses .NET Zip APIs directly instead of `Compress-Archive`.
  - Zip entry names are normalized to `/` separators so the archive is safer to unzip on macOS.
  - The audit now checks that the source package script creates zip entries through `ZipFile` and normalizes entry paths.
- Replaced the mojibake macOS user docs with readable UTF-8 Chinese:
  - `docs\README_MACOS.txt`
  - `docs\SEND_TO_MAC_TESTER.txt`
- Added `scripts\export_macos_support_bundle.sh`, a macOS diagnostics exporter that copies logs, redacts config fields matching API/key/token/secret/password, creates a zip, and avoids any Python dependency.
- Updated `scripts\prepare_macos_release.sh` so the final macOS release folder includes `EXPORT_MACOS_SUPPORT_BUNDLE.sh`.
- Updated `docs\MACOS_PACKAGING.md` and `docs\FINAL_ACCEPTANCE_MATRIX.md` so macOS acceptance includes support-bundle export.
- Updated `scripts\audit_release_readiness.ps1` so Windows-side audit verifies the macOS support exporter and confirms it is copied into the macOS release.
- Regenerated the Windows release folder and both distributable zips after the documentation-template fix.
- Current Windows distributable zip: `release\TeamsCaptionTranslator-v0.2.0-windows.zip`.
- Current Windows distributable zip size: `265,487,439` bytes.
- Current Windows distributable zip SHA256: `E3451FA50D38EB60515B17B210E9EB96F207FA50214B58386EF0CB53FF161DBC`.
- Installer SHA256 is unchanged: `9051D5935653A2123B8CB62551E85316100E248D4DC5367A3B252A1F9FE03E34`.
- `scripts\audit_release_readiness.ps1` now checks the final acceptance matrix and the macOS packaging/release scaffolding.
- `scripts\verify_release.ps1 -OcrSmoke` passed on this machine after the regenerated release:
  - silent install succeeded
  - installed app smoke succeeded
  - first-run UI smoke succeeded
  - OCR preparation smoke succeeded
  - silent uninstall succeeded
- `scripts\audit_release_readiness.ps1 -RunSmoke` passed every package, document, installer, support-exporter, model, macOS-scaffolding, and local install/OCR smoke check. It failed only because this host does not have Windows Sandbox:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- A later audit after adding the macOS support exporter passed all new macOS support-exporter checks and still failed only because this host does not have Windows Sandbox.
- After adding `RELEASE_MANIFEST.json`, `scripts\audit_release_readiness.ps1` confirmed:
  - `Release manifest exists`
  - `Release zip contains release manifest`
  - `Manifest platform is Windows`
  - `Manifest says ordinary users need no Python`
  - `Manifest recommends Windows zip`
  - `Manifest marks macOS as not validated`
  - `Manifest installer hash matches installer`
  - and still failed only because this host does not have Windows Sandbox.
- After regenerating the release zip with `RELEASE_MANIFEST.json`, `scripts\verify_release.ps1 -OcrSmoke` passed again.
- After adding the macOS release manifest and fixing the release-document generator regression:
  - `scripts\prepare_release_package.py` syntax check passed
  - no mojibake markers were found in `scripts\prepare_release_package.py`, `README.md`, or the user-facing docs checked
  - `scripts\prepare_release_package.py` regenerated the Windows release folder and zips
  - `scripts\audit_release_readiness.ps1` passed the new macOS manifest checks and failed only because this host does not have Windows Sandbox
  - `scripts\verify_release.ps1 -OcrSmoke` passed again
- After strengthening macOS EasyOCR model packaging, Windows-side audit confirmed:
  - `macOS build script requires Python 3.10+`
  - `macOS build script prepares EasyOCR models`
  - `macOS build script requires detection model`
  - `macOS build script requires Japanese model`
  - `macOS build script checks bundled app models`
  - `macOS check script verifies bundled OCR models`
  - and still failed only because this host does not have Windows Sandbox.
- `scripts\prepare_macos_source_package.ps1` was tested locally after two compatibility fixes:
  - replaced `[System.IO.Path]::GetRelativePath` with a PowerShell-compatible relative-path calculation
  - replaced broad recursive enumeration with explicit recursion that skips excluded directories before entering them
  - generated `release\TeamsCaptionTranslator-v0.2.0-macos-source.zip`
  - original source package SHA256 before adding source manifest: `32E0068EAE1016B3926F90AE0F16666A3B8149ABC65918EDBF35EDDBDE85A46D`
  - inspected zip entries confirmed local `config.json`, `.venv`, build outputs, dist outputs, release artifacts, and logs were not included
  - release audit passed the new macOS source package checks and still failed only because this host does not have Windows Sandbox.
- After adding `SOURCE_PACKAGE_MANIFEST.json`, `scripts\prepare_macos_source_package.ps1` was run again:
  - source package SHA256 before path-normalized zip rewrite: `EF805CD7E2134F829E2AA634654B3D2FC21415DCC0E9E386405AE17A1274E6CB`
  - inspected zip entries confirmed `SOURCE_PACKAGE_MANIFEST.json` is present and local `config.json`, `.venv`, build outputs, release artifacts, and logs were not included
  - external manifest hash matches the zip hash
  - release audit passed the new source-manifest checks and still failed only because this host does not have Windows Sandbox.
- After rewriting source package zip creation to normalize paths:
  - current source package SHA256: `92D3587739D49F6885A87F1F3AC09889E5BA3E7AAAF146598D013699F9B8F98C`
  - current source package size: `94,092` bytes
  - inspected zip entries: `55`
  - bad backslash-path entries: `0`
  - excluded local entries found: `0`
  - `BUILD_ON_MAC.txt` and `SOURCE_PACKAGE_MANIFEST.json` were present
  - external manifest hash matches the zip hash
  - release audit passed source package manifest and path-normalization checks and still failed only because this host does not have Windows Sandbox.
- The project `.venv` on this workstation is currently stale/broken because its launcher points at a missing system Python path. Source syntax was checked with the Codex isolated Python instead. This does not affect the frozen Windows installer, but local development should repair `.venv` or install a normal Python 3.10+ before running pytest here again.

## Current State

- Installer artifact exists: `installer\TeamsCaptionTranslatorSetup-0.2.0.exe`.
- Current packaged release is Windows-only. It is designed for users without Python installed.
- macOS `.app` / `.dmg` packaging scaffolding now exists, but it has not been built or verified on a real Mac yet.
- Current installer size: `265,962,680` bytes, about `253.6 MB`.
- Current release package exists: `release\TeamsCaptionTranslator-v0.2.0`.
- Current release SHA256: `9051D5935653A2123B8CB62551E85316100E248D4DC5367A3B252A1F9FE03E34`.
- Recommended Windows distributable zip exists: `release\TeamsCaptionTranslator-v0.2.0-windows.zip`.
- Compatibility distributable zip exists: `release\TeamsCaptionTranslator-v0.2.0.zip`.
- Current distributable zip size: `265,487,439` bytes, about `253.2 MB`.
- Current Windows distributable zip SHA256: `331804DDDC127CF18C3BBCE82C2DB29AA0C89C7558BF15BB4EFC85257D456822`.
- The installer has been rebuilt after adding optional DeepSeek smoke support.
- The installer has also been rebuilt after improving the first-run UI guide.
- The installer has been rebuilt again after changing installer-wizard text to ASCII English to reduce Inno Setup encoding risk on other Windows machines.
- The installer has been rebuilt again after making the desktop shortcut task checked by default.
- The release package has been regenerated after that rebuild.
- The release package has been regenerated again after fixing the UTF-8 Chinese text in `QUICK_START.md`.
- The release package now includes `MANUAL_TEAMS_CHECK.md`, an explicit manual Teams rolling-caption acceptance checklist.
- The release package now includes `EXPORT_SUPPORT_BUNDLE.ps1`, which creates a redacted support bundle for nontechnical user troubleshooting.
- The release package now includes `PLATFORM_SUPPORT.md`, which clearly states that the current zip is Windows-only and that macOS must be built and verified separately on a real Mac.
- The release package now includes `SEND_TO_TESTER.txt`, a copy-pasteable Chinese message for normal testers/users. It tells them to use the Windows zip, install, enter their own DeepSeek key, and export a redacted support bundle instead of sending the key.
- The release package now includes `README_CN.txt`, a plain Chinese text quick-start file for users who do not open Markdown files.
- Bundled EasyOCR model files are present in the frozen app:
  - `craft_mlt_25k.pth`
  - `japanese_g2.pth`
- The main remaining validation gap is a real clean Windows machine, Windows Sandbox, or VM test with an actual DeepSeek API key.

## Completed Packaging Work

- `scripts\run.bat` creates and uses the project `.venv`, with self-healing if `.venv\Scripts\python.exe` cannot start.
- `scripts\run.bat` no longer falls back to AstrBot's bundled Python. It now searches normal system Python 3.10+ locations and `py` / `python`.
- `scripts\find_python.ps1` provides a maintainable Python 3.10+ finder for Windows launch/build scripts, excluding AstrBot paths.
- `scripts\build_installer.ps1` now repairs or creates `.venv` through `scripts\run.bat` before building, so a stale/broken venv does not break packaging.
- Runtime config now lives under `%APPDATA%\TeamsCaptionTranslator\config.json`; root `config.json` is only a legacy fallback.
- PyInstaller builds a frozen app through `packaging\teams-caption-translator.spec`.
- Inno Setup builds the one-click installer through `packaging\teams-caption-translator.iss`.
- The Inno installer uses ASCII-safe visible text and creates a desktop shortcut by default.
- `scripts\build_installer.ps1` builds the frozen app and Windows installer.
- `scripts\prepare_release_package.py` generates the distributable release folder, SHA256 file, bilingual quick start, and clean-machine check script.
- The generated `QUICK_START.md` has a readable UTF-8 Chinese quick-start section plus the English fallback section.
- `scripts\prepare_release_package.py` also generates `release\TeamsCaptionTranslator-v0.2.0-windows.zip`, which is the recommended file to send to Windows users.
- `scripts\prepare_release_package.py` also keeps `release\TeamsCaptionTranslator-v0.2.0.zip` as a compatibility copy for older instructions.
- `scripts\prepare_release_package.py` also adds `START_HERE.txt`, a plain-text first file for users who do not open Markdown.
- `scripts\prepare_release_package.py` also adds `README_CN.txt`, a plain Chinese quick-start file for normal Windows users.
- `scripts\prepare_release_package.py` also adds `SEND_TO_TESTER.txt`, a plain-text message maintainers can copy directly to testers.
- `scripts\prepare_release_package.py` also adds `MANUAL_TEAMS_CHECK.md`, which records the final real Teams acceptance steps and pass criteria.
- `scripts\prepare_release_package.py` also adds `PLATFORM_SUPPORT.md`, which describes Windows no-Python support and the separate macOS build/verification path.
- `scripts\prepare_release_package.py` also adds `EXPORT_SUPPORT_BUNDLE.ps1`, which exports logs and a redacted config bundle without copying plain DeepSeek API keys.
- `scripts\prepare_release_package.ps1` now falls back to system Python if the project `.venv` Python launcher exists but cannot start.
- `scripts\verify_release.ps1` performs local install, app smoke, OCR smoke, uninstall, and cleanup verification.
- `scripts\verify_release.ps1` now also runs first-run UI smoke, which verifies that an empty config prompts for a DeepSeek key and shows the full first-use flow.
- `scripts\audit_release_readiness.ps1` audits installer/release consistency and can run local smoke checks.
- `scripts\audit_release_readiness.ps1` also checks that the Inno script is ASCII-safe and that the desktop shortcut task is checked by default.
- `scripts\audit_release_readiness.ps1` also checks that the release zip exists, has a non-trivial size, and contains the installer, quick start, and clean-machine check script.
- `scripts\audit_release_readiness.ps1` also checks that the recommended `-windows.zip` exists and matches the compatibility zip.
- `scripts\audit_release_readiness.ps1` also checks that `START_HERE.txt` exists, is included in the zip, and explains the install/API/OCR/start flow.
- `scripts\audit_release_readiness.ps1` also checks that `README_CN.txt` exists, is included in the zip, and mentions Windows, the recommended zip, Python-free use, DeepSeek, OCR, and support bundle export.
- `scripts\audit_release_readiness.ps1` also checks that `SEND_TO_TESTER.txt` exists, is included in the zip, mentions Windows, DeepSeek, the recommended zip, support bundle export, and warns not to send the key.
- `scripts\audit_release_readiness.ps1` also checks that `MANUAL_TEAMS_CHECK.md` exists, is included in the zip, and covers DeepSeek, OCR, rolling captions, old-content preservation, and speaker formatting.
- `scripts\audit_release_readiness.ps1` also checks that `EXPORT_SUPPORT_BUNDLE.ps1` exists, is included in the zip, and contains redaction, log-copy, and zip-creation logic.
- `scripts\prepare_sandbox_release_check.ps1` creates a Windows Sandbox package for hosts that have `WindowsSandbox.exe`.
- `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1` can independently install the release package, run app smoke, run OCR smoke, optionally run DeepSeek smoke with `-DeepSeekApiKey`, uninstall, and write `CLEAN_MACHINE_RESULT.txt`.
- `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1` also runs first-run UI smoke before OCR smoke.
- The first-run UI now focuses the DeepSeek key box when no key is saved and shows the full user flow: paste key, test DeepSeek, prepare OCR, select subtitle region, start translation.
- `packaging\teams-caption-translator-macos.spec` now defines a macOS PyInstaller `.app` bundle.
- `scripts\build_macos.sh` now creates a macOS-only `.venv-macos`, builds `Teams Caption Translator.app`, and creates `release\TeamsCaptionTranslator-v0.2.0-macos.dmg` when run on a real Mac.
- `scripts\build_macos.sh` now runs platform-diagnostic and first-run UI smoke checks on the generated macOS `.app` before creating the `.dmg`.
- `scripts\check_macos_release.sh` now verifies the generated macOS app executable, platform diagnostic smoke, first-run UI smoke, and `hdiutil verify` on a real Mac.
- `scripts\prepare_macos_release.sh` now prepares the final macOS release folder and zip after real-Mac build/checks pass.
- `docs\README_MACOS.txt` now provides plain Chinese macOS installation and first-run instructions, including Screen Recording permission.
- `docs\SEND_TO_MAC_TESTER.txt` now provides a copy-pasteable message for real Mac testers.
- `scripts\frozen_app.py` now supports `TCT_PLATFORM_DIAGNOSTIC_SMOKE=1`, which writes platform/config/log diagnostics and macOS Screen Recording guidance.
- `src\teams_caption_translator\platform_diagnostics.py` now centralizes macOS Screen Recording and capture/OCR failure hints.
- The UI now shows macOS Screen Recording guidance in first-run text and OCR/capture failure status messages.
- Runtime config/log paths now use `~/Library/Application Support/TeamsCaptionTranslator` on macOS.
- `docs\MACOS_PACKAGING.md` records the macOS build and acceptance checklist, including Screen Recording permission.

## Verification Completed

- Python syntax check passed for:
  - `scripts\frozen_app.py`
  - `scripts\prepare_release_package.py`
- Lightweight regression tests passed with the project `.venv`: `ran 10 tests`.
- After repairing `.venv`, `python -m pytest -q` passed again: `10 passed in 0.24s`.
- `scripts\build_installer.ps1` completed successfully after the DeepSeek smoke source change.
- `scripts\build_installer.ps1` completed successfully again after the first-run UI guide change.
- `scripts\build_installer.ps1` completed successfully again after the installer ASCII text and first-run smoke changes.
- `scripts\build_installer.ps1` completed successfully again after the default desktop shortcut change.
- `scripts\prepare_release_package.ps1` completed successfully after rebuilding the installer.
- `scripts\verify_release.ps1 -OcrSmoke` passed:
  - silent install succeeded
  - installed EXE smoke succeeded
  - installed first-run UI smoke succeeded
  - installed OCR preparation smoke succeeded
  - silent uninstall succeeded
  - install directory cleanup succeeded
- The release clean-machine check script was run directly from `release\TeamsCaptionTranslator-v0.2.0` without a DeepSeek key and passed:
  - cleaned a previous test install
  - installed the release installer
  - ran app smoke
  - ran OCR smoke
  - skipped DeepSeek smoke because no key was supplied
  - uninstalled successfully
  - wrote PASS
- After the first-run UI guide change, the release clean-machine check script was rerun from `release\TeamsCaptionTranslator-v0.2.0` and passed again.
- After adding first-run UI smoke to the release checks, `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1` was rerun and passed again. The log showed `First-run UI smoke exit code: 0`.
- After making the desktop shortcut default, `scripts\verify_release.ps1 -OcrSmoke` and `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1` were rerun and passed again.
- The latest release audit passed the new installer checks:
  - `Installer script visible text is ASCII-safe`
  - `Desktop shortcut task is checked by default`
- After adding the release zip, `scripts\verify_release.ps1 -OcrSmoke`, `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1`, and `scripts\audit_release_readiness.ps1 -RunSmoke` were rerun.
- The latest release audit passed the new zip checks:
  - `Release zip exists`
  - `Release zip size is non-trivial`
  - `Release zip contains installer`
  - `Release zip contains START_HERE.txt`
  - `Release zip contains QUICK_START.md`
  - `Release zip contains clean-machine check`
- After adding `START_HERE.txt`, `scripts\verify_release.ps1 -OcrSmoke`, `release\TeamsCaptionTranslator-v0.2.0\CLEAN_MACHINE_CHECK.ps1`, and `scripts\audit_release_readiness.ps1 -RunSmoke` were rerun.
- The latest release audit passed the new START_HERE checks:
  - `Release START_HERE.txt exists`
  - `START_HERE explains install flow`
  - `START_HERE mentions bundled OCR/Python-free use`
- `scripts\audit_release_readiness.ps1 -RunSmoke` passed every local/release check and failed only on host capability:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- After fixing the `QUICK_START.md` Chinese template, `scripts\prepare_release_package.ps1` completed successfully and regenerated the clean release folder and zip.
- The regenerated `QUICK_START.md` was verified with UTF-8 reading and the Chinese section is readable.
- After that regeneration, `scripts\verify_release.ps1 -OcrSmoke` passed again:
  - silent install succeeded
  - installed EXE smoke succeeded
  - installed first-run UI smoke succeeded
  - installed OCR preparation smoke succeeded
  - silent uninstall succeeded
- The release `CLEAN_MACHINE_CHECK.ps1` also passed again without a DeepSeek key:
  - install succeeded
  - app smoke succeeded
  - first-run UI smoke succeeded
  - OCR smoke succeeded
  - DeepSeek smoke was skipped because no key was supplied
  - uninstall succeeded
- The release package was regenerated once more after that self-check so the distributable folder and zip are clean and do not include generated `clean_*.log` files.
- `scripts\prepare_release_package.py` was cleaned up to remove the stale mojibake quick-start template copies; it now has one UTF-8 quick-start generator.
- `MANUAL_TEAMS_CHECK.md` was added to the release package and verified inside `release\TeamsCaptionTranslator-v0.2.0.zip`.
- `scripts\audit_release_readiness.ps1` was rebuilt as an ASCII-safe audit script so Chinese release docs do not break PowerShell parsing.
- `scripts\audit_release_readiness.ps1 -RunSmoke` passed every package, document, installer, model, and local install/OCR check, and failed only on host capability:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- `EXPORT_SUPPORT_BUNDLE.ps1` was tested with output under `build\support_export_test`:
  - first attempt exposed two robustness issues: CIM access denial and log-copy access denial
  - the exporter was updated to use an OS-version fallback and tolerate log-copy failures
  - the final run created `TeamsCaptionTranslatorSupport-*.zip` successfully
  - the support export test artifact was removed after verification
- After adding the support bundle exporter, `scripts\audit_release_readiness.ps1 -RunSmoke` passed every package, document, installer, support-exporter, model, and local install/OCR check, and failed only on host capability:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- After removing the AstrBot Python fallback, adding `find_python.ps1`, adding macOS packaging scaffolding, and adding `PLATFORM_SUPPORT.md`, the Windows installer was rebuilt successfully.
- `scripts\prepare_release_package.ps1` regenerated the release folder and zip successfully with installer SHA256 `9051D5935653A2123B8CB62551E85316100E248D4DC5367A3B252A1F9FE03E34`.
- `scripts\prepare_release_package.ps1` now generates both `TeamsCaptionTranslator-v0.2.0-windows.zip` and the compatibility copy `TeamsCaptionTranslator-v0.2.0.zip`.
- `scripts\verify_release.ps1 -OcrSmoke` passed after the latest rebuild:
  - silent install succeeded
  - installed EXE smoke succeeded
  - installed first-run UI smoke succeeded
  - installed OCR preparation smoke succeeded
  - silent uninstall succeeded
- `scripts\audit_release_readiness.ps1 -RunSmoke` passed every package, document, platform-support, installer, support-exporter, model, and local install/OCR check, and failed only on host capability:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- The generated release `CLEAN_MACHINE_CHECK.ps1` was run directly without a DeepSeek key after the latest rebuild and passed; DeepSeek smoke was skipped because no key was supplied.
- The release folder and zip were regenerated again after that check so generated `clean_*.log` files are not included in the final distributable zip.
- The final clean release structure audit passed every file/hash/document/model check and failed only on host capability:
  - `[FAIL] Windows Sandbox available on this host`
  - reason: `WindowsSandbox.exe` is not available on this machine.
- After adding the explicit Windows zip name, final release audit confirmed:
  - `Windows release zip exists`
  - `Windows release zip matches compatibility zip`
  - `QUICK_START mentions release zip distribution`
  - `Platform support recommends Windows zip`
  - and failed only on host capability: `WindowsSandbox.exe` is not available on this machine.
- After adding `SEND_TO_TESTER.txt`, release audit confirmed:
  - `Release SEND_TO_TESTER.txt exists`
  - `Release zip contains SEND_TO_TESTER.txt`
  - `SEND_TO_TESTER mentions recommended zip`
  - `SEND_TO_TESTER warns not to send key`
  - `SEND_TO_TESTER mentions support bundle`
  - and failed only on host capability: `WindowsSandbox.exe` is not available on this machine.
- After adding `README_CN.txt`, release audit confirmed:
  - `Release README_CN.txt exists`
  - `Release zip contains README_CN.txt`
  - `README_CN mentions Windows package`
  - `README_CN mentions recommended zip`
  - `README_CN mentions no Python needed`
  - `README_CN mentions DeepSeek`
  - `README_CN mentions OCR`
  - `README_CN mentions support bundle`
  - and failed only on host capability: `WindowsSandbox.exe` is not available on this machine.
- After adding `00_README_CN.txt`, release audit confirmed:
  - `Release 00_README_CN.txt exists`
  - `Release zip contains 00_README_CN.txt`
  - `00_README_CN matches README_CN`
  - and failed only on host capability: `WindowsSandbox.exe` is not available on this machine.
- After adding macOS platform diagnostics and smoke scripts:
  - Python syntax checks passed for `platform_diagnostics.py`, `ui.py`, and `frozen_app.py`
  - `python -m pytest -q` passed: `10 passed`
  - `scripts\build_macos.sh` and `scripts\check_macos_release.sh` were text-inspected on Windows for the expected Darwin guard, platform smoke, first-run smoke, and `hdiutil` verification
  - direct `bash -n` could not run on this host because WSL has no default Linux distribution
  - macOS `.app` / `.dmg` still requires real Mac build and verification
- After adding macOS release/user documentation:
  - `docs\README_MACOS.txt` was checked for Screen Recording, DeepSeek, OCR, Python-free use, and macOS log path guidance
  - `docs\SEND_TO_MAC_TESTER.txt` was checked for Screen Recording, DeepSeek, Test OCR, Python-free use, and "Do not send your DeepSeek Key"
  - `scripts\prepare_macos_release.sh` was text-inspected for Darwin guard, `check_macos_release.sh`, `shasum`, `zip`, `README_MACOS`, `SEND_TO_MAC_TESTER`, and `SHA256`
  - `python -m pytest -q` passed again: `10 passed`
  - actual macOS release zip generation still requires real Mac execution
- After adding macOS returned-package auditing:
  - fixed mojibake in `README.md` and `docs\SEND_TO_MAC_TESTER.txt`
  - added `docs\MAC_TEST_RESULT_TEMPLATE.txt` for traceable real Mac / real Teams manual results
  - added `scripts\audit_macos_release_from_windows.ps1` to inspect a returned `TeamsCaptionTranslator-v0.2.0-macos.zip` from Windows
  - updated `scripts\prepare_macos_release.sh` so macOS release folders include `MAC_TEST_RESULT_TEMPLATE.txt`
  - updated `docs\MACOS_PACKAGING.md` and `docs\FINAL_ACCEPTANCE_MATRIX.md` with the returned-package audit step
  - regenerated the macOS source package:
    - path: `release\TeamsCaptionTranslator-v0.2.0-macos-source.zip`
    - SHA256: `29BEE084EB22306FCE60346B2FB6C3EFC79B31F2B244620101205EEDE199FAE9`
    - size: `99930` bytes
  - `scripts\audit_release_readiness.ps1` now checks the returned-package audit script, result template, and common mojibake markers
  - latest audit passed every package, document, source-package, OCR-model, and macOS-preparation check, and failed only on host capability: `WindowsSandbox.exe` is not available on this machine
- After adding caption stabilization and cleanup controls:
  - added `src\teams_caption_translator\caption_stabilizer.py`
  - added `src\teams_caption_translator\maintenance.py`
  - added config defaults:
    - `caption_stabilize_seconds`: `1.0`
    - `caption_min_chars`: `4`
    - `max_debug_screenshots`: `30`
    - `max_debug_dir_mb`: `100`
  - translation now waits for stable rolling-caption text before sending it to DeepSeek, reducing duplicate translations, broken fragments, and token waste
  - `Test OCR` debug screenshots are pruned after each screenshot save
  - UI includes a `重置数据` action that deletes saved key/config/log/debug data for repeat testing
  - the Inno uninstaller now removes `{userappdata}\TeamsCaptionTranslator`
  - added `tests\test_caption_stabilizer.py`
  - manual test-function run passed for stabilizer, text utils, and caption parser tests
  - rebuilt Windows installer and release zips
  - latest installer SHA256: `592EC1BDCC5FC9FED7F58586638DCD5C1BA70EA5E85AA5A2DCF51AA0587E9D39`
  - latest Windows release zip SHA256: `33587A66DD6762E9E60BD7216324EFF2B36002B5811A54C5AF55D732ECE3F6BA`
  - regenerated macOS source package; the default source zip was locked by another process, so a timestamped fallback was produced:
    - path: `release\TeamsCaptionTranslator-v0.2.0-macos-source-20260606_141043.zip`
    - SHA256: `CFC11DC93D294E4FC30EEB25A3510DC5B66221CCF422CB3AE45C3930B580DC72`
    - size: `104753` bytes
  - `scripts\audit_release_readiness.ps1 -RunSmoke` passed local install, first-run UI, OCR preparation, uninstall, package, document, source-package, and OCR-model checks; it still fails only on host capability: `WindowsSandbox.exe` is not available on this machine

## Remaining Gate

To claim the whole goal is fully complete, run one external clean-machine test:

1. Copy `release\TeamsCaptionTranslator-v0.2.0` to a clean Windows user profile, Windows Sandbox, or VM.
   - For normal distribution, send `release\TeamsCaptionTranslator-v0.2.0-windows.zip` and unzip it on the target machine.
2. Run:

   ```powershell
   powershell -NoProfile -ExecutionPolicy Bypass -File .\CLEAN_MACHINE_CHECK.ps1 -DeepSeekApiKey "sk-..."
   ```

3. Confirm `CLEAN_MACHINE_RESULT.txt` says `PASS`.
4. Also do one manual Teams run:
   - install by double-clicking `TeamsCaptionTranslatorSetup-0.2.0.exe`
   - launch from Start Menu
   - enter DeepSeek API key
   - click `Test DeepSeek`
   - click `Prepare OCR`
   - select the Teams subtitle region
   - click `Test OCR`
   - click `Start Translation`
   - confirm translated rolling captions appear and old content remains.

## Notes

- Do not commit or print any real `config.json` containing a DeepSeek API key.
- Docker is still not the default route for this app because the main workflow needs Windows desktop capture, Teams windows, Tkinter UI, and optional Windows OCR.
